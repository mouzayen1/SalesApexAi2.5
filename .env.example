# ===========================================
# SalesApexAI Environment Variables
# ===========================================
# Copy this file to .env.local for local development
# DO NOT commit .env.local or any file with real credentials

# ===========================================
# DATABASE (Required)
# ===========================================
# PostgreSQL connection string
# For Vercel: Add via Project Settings > Environment Variables
DATABASE_URL=postgresql://user:password@localhost:5432/salesapexai

# ===========================================
# GROQ AI API (Optional - for deal analysis)
# ===========================================
# Get your API key from: https://console.groq.com/keys
# For Vercel: Add via Project Settings > Environment Variables
# IMPORTANT: Use GROQ_API_KEY (not NEXT_PUBLIC_*) to keep it server-side only
GROQ_API_KEY=your_groq_api_key_here

# Optional: Override the default model
# Default: llama-3.1-70b-versatile
GROQ_MODEL=llama-3.1-70b-versatile

# ===========================================
# SERVER (Local development only)
# ===========================================
PORT=3001
NODE_ENV=development

# ===========================================
# CLIENT (Local development only)
# ===========================================
# Vite uses VITE_ prefix for client-side env vars
# This is only used for local dev server proxy configuration
VITE_API_URL=http://localhost:3001
